@model frushgah.Models.subGroup

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_LayoutMain.cshtml";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>subGroup</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.idGroup)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.idGroup)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.nameSubGroup)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.nameSubGroup)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.picSubGroup)
        </dt>

        <dd class="col-sm-10">
            @if (Model.picSubGroup != null)
            {
                string imageBase64 = Convert.ToBase64String(Model.picSubGroup); string
                 imageSrc = string.Format("data:image/gif;base64,{0}", imageBase64);
                <img src="@imageSrc" width="100" height="100" />
            }
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="idSubGroup" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
